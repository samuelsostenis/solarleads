# Exemplo de variáveis de ambiente para desenvolvimento

# ---------- PostgreSQL ----------
DATABASE_URL=postgresql://solarleads:password@localhost:5432/solarleads
PGHOST=localhost
PGPORT=5432
PGUSER=solarleads
PGPASSWORD=password
PGDATABASE=solarleads

# ---------- Backend ----------
PORT=3005
NODE_ENV=development
JWT_SECRET=change_this_to_a_secure_value

# ---------- Frontend (Vite) ----------
VITE_API_URL=http://localhost:3005/api

# ---------- Redis (opcional) ----------
REDIS_URL=redis://localhost:6379

# ---------- Ollama (opcional) ----------
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b

# ---------- WhatsApp service ----------
WHATSAPP_AUTH_PATH=whatsapp-service/auth_info_baileys
WHATSAPP_API_PORT=3002

# ---------- Outros (ajuste conforme necessário) ----------
# NODE_TLS_REJECT_UNAUTHORIZED=0
# LOG_LEVEL=info